# Requirements #

    # once
    $ npm install -g grunt-cli
    $ gem install bundler

    # occasionally after `git pull`
    $ npm install
    $ bundle install

# Run #

    $ grunt
    $ bundle exec jekyll serve    # http://localhost:5000/
